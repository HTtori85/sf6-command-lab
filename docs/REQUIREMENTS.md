# REQUIREMENTS.md - スト6 コマンド入力矯正・分析ツール

## 1. システム概要
本システムは、格闘ゲーム『ストリートファイター6（スト6）』のコマンド入力の「正確さ」と「速さ」を測定・分析・矯正するためのブラウザ完結型Webアプリケーションである。
入力機器（レバーレス、レバー、パッド、キーボード）をブラウザ上で認識し、公式技表等の技コマンドに対する入力ログをリアルタイムで解析する。ユーザーの入力癖（無駄な要素の介入、特定の方向抜けなど）を定量データとして可視化し、矯正練習を可能にする。

---

## 2. コスト・インフラ要件（完全無料運用）
 インフラ Vercel または Cloudflare Pages（フロントエンドホスティング）
 データベース（任意） LocalStorage  IndexedDB（ユーザーブラウザ内保持）または Cloudflare D1  Supabase（無料枠）
 計算・処理 すべてクライアントサイド（ブラウザ上のJavaScript  Web Gamepad API）で完結させること（サーバー代永久0円）。

---

## 3. 入力機器（デバイス）対応要件
ブラウザ標準の Gamepad API および KeyboardEvent を使用し、以下の主要入力デバイスに完全対応する。

 デバイスタイプ  入力検知方式  特記事項 
 ---  ---  --- 
 レバーレス  Gamepad API (DirectInputXInput)  SOCD（同時押し）入力、高速方向切り替えの精度検知 
 レバー（アーケードコントローラー）  Gamepad API  斜め（要素）抜けや余計なニュートラル挟み込みの検知 
 一般コントローラー（パッド）  Gamepad API  D-Pad（十字キー）および アナログスティックの入力変換 
 キーボード  Web KeyboardEvent  レバーレス配置（WASD, Space, テンキー等）のカスタムマッピング 

---

## 4. 機能要件

### A. 公式技表（コマンド）データ取り込み & 管理
1. データソース スト6公式Webサイトの技表（例 ヤスミン等のコマンドリスト）の形式をベースとしたJSON形式でコマンドデータを保持。
2. コマンドパターンのデータ化
    方向要素（1〜9キーテンキー表示 236, 623, 214, 41236, 溜め等）
    ボタン要素（P, K, EXOD, 各ボタン同時押し）

### B. 入力検知 & キーディスプレイ（リアルタイム表示）
1. リアルタイム・キーディスプレイ
    入力された方向（矢印テンキー表記）とボタンを1フレーム（160秒）単位でリアルタイム描画。
2. 入力判定ロジック
    成功判定 指定されたコマンドの必須要素が正しい順序で満たされているか。
    入力時間（フレーム数ミリ秒） コマンド開始から完成までの時間を自動測定。

### C. 入力癖・エラー解析エンジン（コア機能）
ユーザーの誤入力を定量化し、どの「ミス」が多いかを分析・可視化する。

1. エラータイプの自動検知
    余剰入力（ゴミ入力） コマンド内に不要な方向・ボタンが入っている（例 236入力時に「3」や「1」が余分に入る）。
    要素抜け（斜め抜け） 昇竜拳（623）入力時に「2」や「3」が抜けて「63」になっている。
    SOCD同時押しズレ レバーレス特有の逆方向同時押し時の挙動や、ボタンのフレームズレ。
    完成速度の遅延 各ステップ間のフレーム数が長すぎる箇所の特定。
2. エラー分析ダッシュボード
    ミス頻度ランキング 「どの方向が余計に入りがちか」「どの斜めが抜けやすいか」をパーセンテージとグラフで表示。
    デバイス別プロファイル レバーレスレバーパッドごとの入力傾向を比較・記録。

### D. 練習・トレーニングモード
1. 反復練習モード 特定技のコマンドを連続入力し、成功率・平均速度・ミス傾向を集計。
2. 苦手矯正モード 過去のデータから「ミスの多いコマンド箇所」のみを重点的に練習させる。

---

## 5. 技術スタック
 Framework Next.js (App Router) または React (Vite)
 Styling Tailwind CSS
 Input handling Gamepad API, `keydown`  `keyup` イベントリスナー
 State  Storage Zustand  React Context, LocalStorage (設定・練習ログ保存用)

---

## 6. 実装ロードマップ（Claude Code 実行手順）
Claude Codeで実装を進める際は、以下のステップ順にプロンプトを出して1機能ずつ構築すること。

 Step 1 最小入力プロトタイプの作成
   Gamepad APIおよびキーボードイベントから入力を取得し、画面上に格ゲー風キーディスプレイ（入力履歴）をスクロール表示するコンポーネントを作成する。
 Step 2 コマンドデータ構造（JSON）と判定ロジックの実装
   波動拳（236P）や昇竜拳（623P）等の判定ロジックを作成し、成功失敗および所要フレーム数を計測できるようにする。
 Step 3 エンド判定 & エラー解析エンジンの実装
   入力ログから「不要な要素」「抜け」を割り出し、エラー集計用データオブジェクトを生成するロジックを組む。
 Step 4 分析ダッシュボード UI の構築
   練習結果から「どのキーが多く入ってしまっているか」「どのミスが多いか」をグラフや数値で可視化するUIを作成する。
 Step 5 キャラ別コマンド（ヤスミン等）のデータ拡充 ＆ 設定機能追加
   コントローラーのボタンマッピング変更機能およびキャラ別技表の切り替え機能を追加する。